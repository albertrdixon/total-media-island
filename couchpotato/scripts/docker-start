#!/bin/bash
set -eo pipefail
trap "exit 0" 2 15

echo "==> Starting dnsmasq, I hope you gave me NET_ADMIN..."
dnsmasq --all-servers --local-service

cd $COUCHPOTATO_HOME
git checkout $COUCHPOTATO_CHANNEL
git pull --verbose

exec -a "supervisord" "supervisord" "-c" "/etc/supervisor/supervisord.conf"
exit 0
